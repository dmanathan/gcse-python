cars = []
sentry = True
while sentry == True:
    name = input("car brand or press x to exit")
    if name == "x":
        print("exiting")
        sentry = False
    else:
        cars.append(name)
print("In the list is", cars)
